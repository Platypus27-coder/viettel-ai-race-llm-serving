# Controlled candidate: speculative-draft
# Parent candidate: v6-incumbent
# Parent Compose SHA-256: 2a9b7c682ff3829630b9938a76f21ec43e88e0e8f4ec0122ee135b0d19f5e348
# Change: v6 with the baked local LFM2.5-350M draft model (4 draft tokens).
# ============================================================
# Viettel AI Race 2026 — SUBMISSION FILE
# ============================================================
# Model: LiquidAI/LFM2.5-1.2B-Instruct
# GPU: 1x MiG H200 (18GB VRAM, 3 CPU cores, 8GB RAM)
# Framework: vLLM v0.22.1
#
# PROFILE: v6.0 (BEST PERFORMING SUBMISSION — SCORE 61.41)
#
# Score Breakdown:
#   Score: 61.4100
#   TTFT p50: 45ms
#   TTFT p95: 69ms
#   Failed requests: 5 / 420 (98.8% success)
#   Accuracy drop: 0 (f_delta = 1.0)
# ============================================================

services:
  model:
    image: yahiisenpai/viettel-ai-vllm@sha256:891d944c92567a56178f083879193730fb5a5faea8743031a1f8beef66a0eb2c

    entrypoint:
      - python3                             # DO NOT CHANGE
      - -m                                  # DO NOT CHANGE
      - vllm.entrypoints.openai.api_server  # DO NOT CHANGE

    command:
      # ---- Core (DO NOT CHANGE) ----
      - --model=/model
      - --served-model-name=LFM2.5-1.2B-Instruct
      - --host=0.0.0.0
      - --port=8000
      - --tensor-parallel-size=1

      # ---- Memory & Cache ----
      - --max-model-len=8192
      - --gpu-memory-utilization=0.97

      # ---- Quantization ----
      - --quantization=fp8
      - --kv-cache-dtype=fp8_e4m3

      # ---- Caching ----
      - --enable-prefix-caching
      - '--speculative-config={"method":"draft_model","model":"/opt/draft/LFM2.5-350M","num_speculative_tokens":4,"draft_tensor_parallel_size":1,"max_model_len":8192}'

    ports:
      - "8000:8000"
    shm_size: "4g"

    environment:
      - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
      - OMP_NUM_THREADS=1
      - MKL_NUM_THREADS=1
      - VLLM_NO_USAGE_STATS=1
      - DO_NOT_TRACK=1
      - VLLM_LOGGING_LEVEL=WARNING

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
